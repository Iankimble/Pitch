const express = require("express");

const {} = require();

const router = express.Router();

// route to GET user portfolio images by ID

// route to PUT (edit) user portfolio images by ID

// route to DELETE user portfolio images by ID

//

module.exports = router;
