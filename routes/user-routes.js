const express = require("express");
const {} = require();

const router = express.Router();
const {} = require();

// route to get user by ID
// route to get user photo from ID

// route to update user profile by ID

// route to delete user profile by ID

// route to get another user profile by ID

// route to get all user portfolio images by user ID

// route to get user follwers and user followings

module.exports = router;
